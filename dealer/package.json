{
  "name": "@pushflip/dealer",
  "description": "Workspace-internal ZK dealer for the pushflip program. Consumed in source form via tsx; not intended for publishing. Run `pnpm build` first if you need a compiled artifact under dist/.",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "npx tsx src/index.ts",
    "service": "npx tsx src/service.ts",
    "typecheck": "tsc --noEmit",
    "test": "find src -name '*.test.ts' -exec npx tsx --test {} +"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.0",
    "@pushflip/client": "workspace:*",
    "@solana-program/compute-budget": "^0.15.0",
    "@solana/kit": "^6.8.0",
    "circomlibjs": "^0.1.7",
    "hono": "^4.10.0",
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@types/snarkjs": "^0.7.8",
    "tsx": "^4.19.0",
    "typescript": "~6.0.2"
  }
}
